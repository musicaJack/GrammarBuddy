# Copy to mySite: ngx/conf.d/00-websocket-map.conf
# (included in http { } via conf.d/*.conf — required for WebSocket proxy)

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
